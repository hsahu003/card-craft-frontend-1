# Allow only built dependencies (core-js, msw, sharp, etc.)
only-built-dependencies=true

# Disable strict peer dependency checks (optional)
strict-peer-dependencies=false

# Whitelist specific packages (optional)
allowed-built-dependencies[]=core-js
allowed-built-dependencies[]=msw
allowed-built-dependencies[]=sharp